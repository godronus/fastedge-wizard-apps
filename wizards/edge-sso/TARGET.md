# Wizard intake: edge-sso

> Generated by `/wizard-intake`. The param source of truth is
> `fastedge.templates.read` (see param tables below). Runtime template
> identification uses `launchTemplateId` / `companionTemplateIds` from
> `context.get()` — the IDs below are for *your* understanding only;
> the shipped wizard must not hard-code them.

## Target templates

| ID  | Name                | api_type   | binary_id | Role                          |
|-----|---------------------|------------|-----------|--------------------------------|
| 748 | mySSO - CDN Filter  | proxy-wasm | 822162    | **Launch** — carries `WIZARD_SOURCE_CONFIG`, real params (not a placeholder) |
| 747 | mySSO - Auth App    | wasi-http  | 822161    | Companion                     |

> **Supersedes the previous 737/738–743 architecture below.** The wizard
> implementation was consolidated after this doc was first written: instead
> of 6 separate per-variant templates plus an inert 7th launch placeholder,
> the shipped shape is **2 unified templates** (one auth-app, one CDN filter),
> each parameterized by a single `SSO_VARIANT` enum (`gate-only` / `cookie` /
> `header`) rather than by picking which of 6 template IDs to target. Both
> 747 and 748 have real, non-empty `params` — neither is an inert
> placeholder. 748 (CDN Filter) was confirmed as the launch template by the
> user directly (a portal-UX choice, launching from the CDN side); 747 is its
> sole companion. `WIZARD_SOURCE_CONFIG` on 748 sets
> `companionTemplateIds: [747]`. The rest of this document (variant-picker
> params, per-old-template-ID sections) describes the superseded design and
> is kept for history — the "Params — mySSO" sections below reflect the
> current live templates.

## Params — mySSO - Auth App (id: 747, wasi-http)

Same param set as the superseded id-738 section below, minus the four
`*_API_BASE_URL`/`*_OAUTH_BASE_URL`/`*_API_VERSION`/`GOOGLE_JWKS_URL` naming
being per-provider rather than grouped, plus the ES256 signing pair
(`SESSION_SIGNING_KEY` / `SESSION_PUBLIC_JWK`, both `mandatory: false` now —
required only when `SSO_VARIANT` is `cookie`, per param `descr`). All three
variants (gate-only / cookie / header) are selected via the single
`SSO_VARIANT` param (`mandatory: true`) instead of by choosing which template
to deploy. `SESSION_SECRET` is `mandatory: true` here (used by every
variant to sign OAuth/SAML flow cookies; also the HS256 signing key for
gate-only/header). `SSO_AUDIENCE` is `mandatory: true`. Full provider
(Google/GitHub/Microsoft/Facebook), SAML, and login-page-branding params are
unchanged from the superseded id-738 section's description below.

## Params — mySSO - CDN Filter (id: 748, proxy-wasm)

| Param | data_type | mandatory | default | descr |
|-------|-----------|-----------|---------|-------|
| `SSO_AUDIENCE` | string | **true** | | Must equal the auth-app's value. Fail-closed. |
| `SSO_VARIANT` | string | **true** | | `gate-only` / `cookie` / `header`. Must be identical to the auth-app's value. |
| `SESSION_SECRET` | secret | false | | Required when `SSO_VARIANT` is `gate-only` or `header`. Must equal the auth-app's value. |
| `SESSION_PUBLIC_JWK` | string | false | | Required when `SSO_VARIANT` is `cookie`. Must be the exact public counterpart of the auth-app's `SESSION_SIGNING_KEY`. |
| `SSO_ISSUER` | string | false | | Must match auth-app if set. |
| `AUTH_PREFIX` | string | false | `/auth` | Path prefix the filter bypasses. Must align with the CDN rule routing this prefix to the auth-app. |
| `LOGIN_PAGE_URL` | string | false | `AUTH_PREFIX/` | Redirect target for unauthenticated users. |
| `SESSION_COOKIE` | string | false | `sso_session` | Must match the auth-app. |

---

## Superseded design (737/738–743) — kept for history only

## Params — SSO - Gate-Only Auth App (id: 738, wasi-http)

| Param | data_type | mandatory | default | descr |
|-------|-----------|-----------|---------|-------|
| `SESSION_SECRET` | secret | **true** | | Random 32-byte base64url string. Signs OAuth/SAML flow cookies in all variants; also signs HS256 session tokens in gate-only and header variants. Same value as the CDN filter. |
| `SSO_AUDIENCE` | string | **true** | | Token `aud` claim. Fail-closed — filter rejects everything if unset or mismatched. Must equal the CDN filter's value. |
| `SSO_ISSUER` | string | false | | Token `iss` claim. Validated only if set on both apps; must match if set. |
| `SSO_PROVIDERS` | string | false | | Comma list of providers to show (`google,github,microsoft,facebook,saml`). Omit = show all configured. |
| `SSO_CLAIMS` | string | false | | Comma list of extra identity claims beyond `sub`. Meaningless for gate-only (no delivery); kept for parity with cookie/header. |
| `SSO_ALLOWED_ORIGINS` | string | false | | Comma list of absolute origins allowed as `?redirect=` targets. Empty = relative-only (safe default). |
| `SESSION_COOKIE` | string | false | `sso_session` | Session cookie name. Must match the CDN filter. |
| `CANONICAL_HOST` | string | false | | Enforced via 301; keeps IdP callback URLs stable. Recommended: set to the deployment host. |
| `GOOGLE_CLIENT_ID` / `GOOGLE_CLIENT_SECRET` (secret) / `GOOGLE_REDIRECT_URI` | string/secret | false | | Google OAuth. Provider self-activates only if creds present. |
| `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET` (secret) / | string/secret | false | | GitHub OAuth. |
| `MICROSOFT_CLIENT_ID` / `MICROSOFT_CLIENT_SECRET` (secret) / `MICROSOFT_REDIRECT_URI` / `MICROSOFT_TENANT` (default `common`) / `MICROSOFT_ALLOWED_TENANTS` | string/secret | false | | Microsoft OIDC. Wildcard tenant strongly recommends setting `MICROSOFT_ALLOWED_TENANTS`. |
| `FACEBOOK_CLIENT_ID` / `FACEBOOK_CLIENT_SECRET` (secret) / `FACEBOOK_REDIRECT_URI` | string/secret | false | | Facebook OAuth. |
| `IDP_SSO_URL` / `IDP_ENTITY_ID` / `IDP_CERT` (secret) / `SP_ENTITY_ID` / `SP_ACS_URL` / `IDP_LABEL` (default `SSO`) / `IDP_ICON_URL` / `SAML_CLAIM_MAP` | string/secret | false | | SAML 2.0. |
| `LOGIN_PAGE_TITLE` / `_SUBTITLE` / `_LOGO_URL` / `_FAVICON_URL` / `_ACCENT_COLOR` (default `#0066cc`) / `_BACKGROUND_COLOR` (default `#f0f2f5`) / `_CSS_URL` | string | false | | Hosted login page branding (Tier 1/2 — see integration.md). |
| `*_API_BASE_URL` / `*_OAUTH_BASE_URL` / `*_API_VERSION` / `GOOGLE_JWKS_URL` (various providers) | string | false | | Test-only endpoint overrides. Not customer-facing. |

## Params — SSO - Gate-Only CDN Filter (id: 739, proxy-wasm)

| Param | data_type | mandatory | default | descr |
|-------|-----------|-----------|---------|-------|
| `SESSION_SECRET` | secret | **true** | | Must equal the auth-app's value — HS256 verification key. |
| `SSO_AUDIENCE` | string | **true** | | Must equal the auth-app's value. Fail-closed. |
| `SSO_ISSUER` | string | false | | Must match auth-app if set. |
| `AUTH_PREFIX` | string | false | `/auth` | Path prefix the filter bypasses. Must align with the CDN rule routing this prefix to the auth-app. |
| `LOGIN_PAGE_URL` | string | false | `AUTH_PREFIX/` | Redirect target for unauthenticated users. Override only for a fully custom login page (Tier 2). |
| `SESSION_COOKIE` | string | false | `sso_session` | Must match the auth-app. |

## Params — SSO - Cookie Auth App (id: 740, wasi-http)

Same superset of provider/branding/SAML params as id 738, plus the ES256 signing pair:

| Param | data_type | mandatory | default | descr |
|-------|-----------|-----------|---------|-------|
| `SESSION_SIGNING_KEY` | secret | **true** | | EC P-256 private key, PKCS#8 PEM. Signs ES256 tokens. Generate with the keypair button — never separately from `SESSION_PUBLIC_JWK`. |
| `SESSION_PUBLIC_JWK` | string | **true** | | Public half of the same EC P-256 keypair, JWK JSON. Must be the exact public counterpart of `SESSION_SIGNING_KEY`. Also required on the CDN filter. |
| `SESSION_SECRET` | secret | **true** | | Still required — signs the OAuth/SAML *flow* cookies (state/PKCE), even though the session token itself is ES256-signed. Not shared with the filter in this variant (filter doesn't need it). |
| `SSO_AUDIENCE` | string | **true** | | Same as gate-only. |
| ...all other params identical to id 738... | | | | |

## Params — SSO - Cookie CDN Filter (id: 741, proxy-wasm)

| Param | data_type | mandatory | default | descr |
|-------|-----------|-----------|---------|-------|
| `SESSION_PUBLIC_JWK` | string | **true** | | Public JWK used to verify ES256 tokens. Must be the exact public counterpart of the auth-app's `SESSION_SIGNING_KEY`. |
| `SSO_AUDIENCE` | string | **true** | | Must equal the auth-app's value. |
| `SSO_ISSUER` | string | false | | Must match auth-app if set. |
| `AUTH_PREFIX` | string | false | `/auth` | Same as gate-only filter. |
| `LOGIN_PAGE_URL` | string | false | `AUTH_PREFIX/` | Same as gate-only filter. |
| `SESSION_COOKIE` | string | false | `sso_session` | Same as gate-only filter. |

> Note: the cookie CDN filter does **not** take `SESSION_SECRET` — it verifies ES256 via the public JWK only, never holds a forge-capable secret.

## Params — SSO - Header Auth App (id: 742, wasi-http)

Identical param set to id 738 (gate-only auth-app) — same HS256 signing via `SESSION_SECRET`. The only functional difference (header injection) is compiled into the CDN filter, not configured on the app.

## Params — SSO - Header CDN Filter (id: 743, proxy-wasm)

Identical param set to id 739 (gate-only filter): `SESSION_SECRET` (secret, **true**), `SSO_AUDIENCE` (**true**), `SSO_ISSUER`, `AUTH_PREFIX`, `LOGIN_PAGE_URL`, `SESSION_COOKIE`. Behaviorally this filter additionally injects `x-sso-*` identity headers upstream (compile-time feature, no extra param).

## Cross-app constraints

> Derived from: param `descr` text (explicit "must match" wording, consistent
> across all three variants) + source repo `context/architecture/overview.md`
> (Token contract, Signing strategy sections).

Explicit (stated verbatim in every relevant param's `descr`):
- `SSO_AUDIENCE` — must be identical on auth-app and CDN filter for the chosen variant. Fail-closed: filter rejects **all** tokens if unset.
- `SESSION_COOKIE` — must be identical on both apps (default `sso_session`).
- `SSO_ISSUER` — if set, must be identical on both apps (validated only when both set it).
- **cookie variant only**: `SESSION_PUBLIC_JWK` on the CDN filter must be the exact public counterpart of `SESSION_SIGNING_KEY` on the auth-app (asymmetric keypair, not a shared secret).
- **gate-only and header variants**: `SESSION_SECRET` must be identical on auth-app and CDN filter (shared HMAC key, HS256).

Inferred (stated in source-repo prose, not literally in a param `descr`):
- `AUTH_PREFIX` on the CDN filter must align with whatever CDN rule routes that path prefix to the auth-app (overview.md, "Cross-domain — Option 3"). Not itself duplicated on the auth-app as a param, but the auth-app's routes live under this same prefix by convention (`/auth/**`).
- The CDN filter **must bypass** the auth prefix — otherwise the login flow redirect-loops (overview.md).

## Secrets

| Secret | Variant(s) | How obtained |
|--------|-----------|--------------|
| `SESSION_SECRET` | gate-only, header (both apps); cookie (auth-app only, signs flow cookies) | Wizard-defined random key → `secrets.pickOrCreate({ name, bytes: 32 })`. Bind the same secret id to both apps in gate-only/header. |
| `SESSION_SIGNING_KEY` + `SESSION_PUBLIC_JWK` | cookie only (auth-app private key + CDN filter public JWK) | Asymmetric keypair → `secrets.generateKeypair({ name, algorithm: "ES256" })`. Private half bound to auth-app as secret; public JWK value written as a plain env var on **both** apps. |
| `GOOGLE_CLIENT_SECRET`, `GITHUB_CLIENT_SECRET`, `MICROSOFT_CLIENT_SECRET`, `FACEBOOK_CLIENT_SECRET` | all variants, per enabled provider | User brings it (pasted external OAuth client secret) → `secrets.pickOrCreate()`. Optional — provider self-activates only if its secret is present. |
| `IDP_CERT` | all variants, if SAML enabled | User brings it (pasted IdP signing cert) → `secrets.pickOrCreate()`. |

No `data_type: "secret"` param is mandatory except `SESSION_SECRET` (gate-only/header) and `SESSION_SIGNING_KEY` (cookie) — both are the session-signing key and always required for whichever variant is chosen. All OAuth/SAML provider secrets are `mandatory: false` at the API level but are the de facto trigger for enabling that provider (see Profile/variant branches).

## Edge Storage

None. The source repo README/overview.md is explicit: **"KV is explicitly NOT used for config — too expensive per read."** No `data_type: "store"` params exist on any of the six templates. Unlike edge-totp, this wizard has no Edge Storage step.

## CDN wiring

Per `context/architecture/overview.md` §"Cross-domain — Option 3 (single domain via CDN-as-origin)":
- The **auth-app** is added as a CDN origin.
- A CDN rule routes the `AUTH_PREFIX` path prefix (default `/auth/**`) to the auth-app origin; everything else continues to the customer's existing origin.
- The **cdn-filter** is the CDN app / rule that gates all non-`AUTH_PREFIX` traffic, checking the session cookie.
- The filter's `AUTH_PREFIX` value and the CDN rule's routed prefix must be the same string — this is a wizard-authored CDN rule, not a template param, so the wizard must write it consistently when it creates the rule.
- IdP OAuth/SAML callback and ACS URLs are registered against this single CDN domain (`CANONICAL_HOST`) — stable across redeploys.

## Profile / variant branches (current: single companion pair, `SSO_VARIANT` param)

> Note: this section originally described picking 2-of-6 companion template
> IDs. That's superseded — there is now exactly one companion pair (747 +
> 748, from `context.get().companionTemplateIds`), and the variant choice is
> written as the `SSO_VARIANT` param value on both, not as a template
> selection. The functional branches below (provider selection, cookie-only
> keypair step, branding) are unchanged.

1. **Variant picker** (first step): gate-only / cookie / header. Sets `SSO_VARIANT` identically on both 747 (auth-app) and 748 (CDN filter).
   - gate-only → HS256 via shared `SESSION_SECRET`. Delivers nothing to origin; only pass/fail.
   - cookie → ES256 via `SESSION_SIGNING_KEY` (747) / `SESSION_PUBLIC_JWK` (both). Delivers a verifiable JWT cookie.
   - header → HS256 via shared `SESSION_SECRET`, same as gate-only. Delivers `x-sso-*` headers to origin.
2. **Provider selection** (per variant, same for all three): user picks one or more of Google / GitHub / Microsoft / Facebook / SAML. Each selected provider makes its client-id/secret (and, for SAML, the IdP_* block) required in practice even though the API marks them `mandatory: false` — this is the fail-closed-in-practice case the intake instructions call out explicitly.
   - If Microsoft is selected and `MICROSOFT_TENANT` is left as the wildcard default (`common`/`organizations`/`consumers`), the wizard should nudge the user toward setting `MICROSOFT_ALLOWED_TENANTS` (security-relevant default called out in the param `descr` itself).
3. **cookie-only sub-step**: generate the ES256 keypair (`secrets.generateKeypair`) instead of the random-bytes secret used by the other two variants.
4. **Branding** (optional, all variants): `LOGIN_PAGE_*` tier-1 env vars. Purely cosmetic, safe to skip/default.

## Notes

- **Superseded (2026-08-13):** the 737/738–743 seven-template architecture
  described above was never shipped as-is. The live templates are 747
  (auth-app) and 748 (CDN filter) — confirmed live via
  `GET /fastedge/v1/template/{747,748}` (API status 200 each) during
  `/wizard-publish`. Both have real, non-empty `params` (no inert
  placeholder template exists in the current design). 748 is the launch
  template (carries `WIZARD_SOURCE_CONFIG`, `companionTemplateIds: [747]`),
  confirmed directly by the user as a portal-UX choice (launching from the
  CDN side) rather than something documented anywhere in template metadata.
  The variant choice (gate-only / cookie / header) is now a single
  `SSO_VARIANT` param value written identically to both templates, not a
  template-selection decision — there is no more "which 2 of 6 companion IDs"
  branch.
- All prior-note claims about `core/` code sharing across six templates and
  the "pick 2 of N companions" wizard step describe the superseded design;
  treat the "Superseded design" section above as historical only.
